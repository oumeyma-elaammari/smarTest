<Window x:Class="smartest_desktop.Views.StatistiquesProfWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:smartest_desktop.Views"
        Title="SmarTest — Statistiques et résultats"
        Icon="/Assets/SmarTest-AppIcon.png"
        Height="720" Width="1280"
        MinHeight="600" MinWidth="960"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBgBrush}"
        FontFamily="Segoe UI">


	<Window.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="/Views/Styles/StylesGlobal.xaml"/>
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Window.Resources>

	<Grid>
		<!-- ══ CONTENU ══ -->
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Background="{DynamicResource WindowBgBrush}">
				<views:StatistiquesDashboardView x:Name="Dashboard" Margin="40,36,40,40"/>
			</ScrollViewer>
		</Grid>
	</Grid>
</Window>
