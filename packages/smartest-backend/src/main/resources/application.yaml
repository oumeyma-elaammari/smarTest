spring:
  application:
    name: smarTest-backend

  datasource:
    url: jdbc:mysql://localhost:3306/smartest_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    baseline-on-migrate: false
    locations: classpath:db/migration

  mail:
    host: smtp.gmail.com
    port: 587
    username: smarttest320@gmail.com
    password: ysvf dmuq jnyo jrxg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8081

app:
  jwt:
    secret: SmArTeSt_S3cr3t_K3y_2026_ENSA_ChangeMe!
    expiration: 86400000

  websocket:
    allowed-origins: http://localhost:5173

  web:
    # Lien affiché dans l’email « nouveau quiz publié » (espace étudiant web)
    dashboard-url: http://localhost:5173/dashboard

  flyway:
    # Dev : true une fois après un échec de migration (repair), puis false.
    repair-before-migrate: false