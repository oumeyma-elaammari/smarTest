spring:
  application:
    name: smarTest-backend
    server:
      port:8080
  datasource:
    url: dbc:mysql://localhost:3030/smartest_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: smartest
    password: smartest
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
      show-sql: false
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

app:
  jwt:
    secret: SmArTeSt_S3cr3t_K3y_2025_ENSA_ChangeMe!
    expiration: 86400000
  websocket:
    allowed-origins: http://localhost:5173


